server:
  port: 8080
  servlet:
    context-path: /profile
spring:
  application:
    name: profile-service
  data:
    mongodb:
      uri: mongodb://root:root@localhost:27017/profile-service?authSource=admin

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri:

idp:
  url: http://localhost:8180
  client-id: KeyClockRealm_app_id
  client-secret: 6dIZy5fopTn61B0FrFFRsS1TOV50TLWp
